version: '3.8'

services:
  mongo:
    image: mongo:6
    container_name: mongo_logs
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: logs_db

volumes:
  mongo_data: